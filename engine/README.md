# Engine Layer

The engine layer documents reusable filmmaking responsibilities. Current Python
implementation remains in `src/ai_film_studio`; these folders define the V3 module
map for future extraction without putting project story content inside the engine.
